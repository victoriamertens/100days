const fetchCoordinates = (city) => {
  console.log('In Coorinates', city);
  return 0;
};

const fetchWeather = (coordinates) => {
  console.log('In weather', coordinates);
  return 0;
};

const utilsExport = { fetchCoordinates, fetchWeather };

export default utilsExport;
